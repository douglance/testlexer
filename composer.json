{
    "name": "splitprops/test",
    "description": "Test suite comparing heuristic vs lexer splitProps implementations",
    "type": "project",
    "require-dev": {
        "phpunit/phpunit": "^11.0"
    },
    "autoload": {
        "psr-4": {
            "SplitProps\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "SplitProps\\Tests\\": "tests/"
        }
    },
    "scripts": {
        "test": "phpunit",
        "test:verbose": "phpunit --testdox"
    }
}
